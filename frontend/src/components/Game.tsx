import React from 'react'
import { Deck } from '../types'

interface Props {
    deck: Deck    
}

export const Game = ({deck}: Props) => {
    return (
        <div>
            
        </div>
    )
}
